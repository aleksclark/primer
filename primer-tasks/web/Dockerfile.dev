# syntax=docker/dockerfile:1

FROM node:22-alpine

WORKDIR /src/web
COPY package.json package-lock.json ./
# Immutable bootstrap: never rewrite a lockfile mounted from the worktree.
RUN npm ci

COPY . .

EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
