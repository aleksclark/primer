# clients/ts-rest

Generated TypeScript authoring REST client package (openapi-typescript +
openapi-fetch pattern, matching LMS pins as a starting point).

- Sources: emitted/baseline OpenAPI (see C6–C7)
- Build-only output: `generated/` (gitignored)
- Consumers (Studio UI) import this package only — no raw `fetch` to `/studio/v1`
