# Logo assets

Place approved SVG files here using the names in the parent README. The preview falls back to a temporary “P” tile until `logo.svg` and `logo-light.svg` are supplied.

Keep original editable artwork outside generated directories. Do not commit raster-only masters.
