# Phase 4 student dialogue evidence (sanitized)

## Manual Terra exploration

Fresh isolated Terra runs on committed tips verified incrementally:

- default parent authoring, publish, schedule, fresh pairing, and Start task;
- configured-source first evaluation;
- two accepted answers with questions advancing wall → mortar → rushing;
- reconnect at 2/3 preserving state;
- insufficient and injection/answer-key answers rendering durable retry state;
- final source-grounded completion with one completion entry.

The complete required matrix was not reached. Remaining unrun gates include foreign/cross-task denial, concurrent web/app recovery, malformed/timeout retry, parent inspect plus immediate override reconciliation, responsive light/dark/mobile/axe, and complete wire/DB/log privacy review. No Playwright promotion occurred.

The latest fresh run was blocked by the dedicated Chrome DevTools MCP shared-profile process before authoring could continue; prior fresh runs were stopped fail-closed at the defects above. No QR material, pairing codes, cookies, tokens, raw snapshots, raw inspect payloads, bulky logs, or model reasoning are retained.
